<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowActionBarOverlay">true</item>
    </style>

    <style name="SettingsTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/ActionBar.PopMovies.Notitle</item>
    </style>

    <!-- Settings activity action bar styles -->
        <style name="ActionBar.PopMovies.Notitle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
            <item name="android:background">@color/abc_primary_text_material_dark</item>
            <item name="android:height">56dp</item>
        </style>
</resources>
